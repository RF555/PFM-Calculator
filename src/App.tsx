import { TestBed } from "./harness/TestBed";

/**
 * Harness only. The deliverable is src/calculator/ — everything here exists
 * to exercise it before handoff.
 */
export default function App() {
  return <TestBed />;
}
